#include "Arf_BLE.h"

void sendDataBLE(const char *data) {
    // pCharacteristic->setValue(data);
    // delay(100);
    // pCharacteristic->notify();
    // delay(100);
    // Serial.println("Sent data to BLE");
}